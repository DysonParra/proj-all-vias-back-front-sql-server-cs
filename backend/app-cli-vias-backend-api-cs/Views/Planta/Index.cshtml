@model IEnumerable<Project.Models.Planta>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StrNombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StrDetalles)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StrNombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StrDetalles)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StrCodigo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.StrCodigo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.StrCodigo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
